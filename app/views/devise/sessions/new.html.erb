<div class="coontainer">
  <h2 class="h2-sign">CONNEXION</h2>
 
  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="form">
      <%= f.email_field :email, class: "input", placeholder: "Email", required: true %>
      <%= f.password_field :password, class: "input", placeholder: "Mot de passe", required: true %>
      <span class="forgot-password"> <%= link_to 'Mot de passe oublié ?', new_user_password_path %> </span>
      <%= f.submit "LOG IN", class: "login-button" %>
    </div>
    <p class="p-log">Pas encore chez nous ? &nbsp <%= link_to 'INSCRIPTION', new_user_registration_path %> &nbsp Clients</p>

    <p class="p-log"> Ou &nbsp <%= link_to 'INSCRIPTION', new_admin_registration_path %> &nbsp Professionnels</p>
  <% end %>
</div>

